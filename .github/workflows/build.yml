name: Build macOS App

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Python 3.14, Tk, and SDL2
        run: brew install python@3.14 python-tk@3.14 sdl2 sdl2_image sdl2_mixer sdl2_ttf portmidi

      - name: Build app
        run: bash build_app.sh

      - name: Ad-hoc sign app
        # Required for Apple Silicon; lets recipients open via right-click → Open
        run: codesign --deep --force --sign - "dist/Waveform Matched Sampler.app"

      - name: Package .app for upload
        # ditto preserves macOS resource forks, symlinks, and extended attributes
        run: |
          ditto -c -k --sequesterRsrc --keepParent \
            "dist/Waveform Matched Sampler.app" \
            "dist/Waveform Matched Sampler.zip"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Waveform-Matched-Sampler-macOS
          path: dist/Waveform Matched Sampler.zip
          retention-days: 30
